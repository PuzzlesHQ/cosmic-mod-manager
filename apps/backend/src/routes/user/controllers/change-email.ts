// import { generateRandomToken, hashString } from "~/routes/auth/helpers";
// import type { UserSessionData } from "~/types";

// export async function sendEmailChangeCode(user: UserSessionData, newEmail: string) {
//     const code = generateRandomToken(8);
//     const codeHash = hashString(code);
// }
